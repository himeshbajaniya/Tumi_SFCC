'use strict';

var processInclude = require('base/util');

$(document).ready(function () {
    processInclude(require('./components/commonComponent'));
    // processInclude(require('./product/compare'));
});
